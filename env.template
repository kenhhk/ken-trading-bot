# Ken's Trading Bot — Environment Variables Template
# DO NOT fill in real values here. DO NOT commit a .env file.
# Set these as environment variables in Claude Code Cloud Routine settings.
# For local testing only: copy to .env and fill in values (never commit).

# ── Alpaca — Account A (Claude Swing Trade / PA34GSDDFIEO) ──────────────────
ALPACA_KEY_A=
ALPACA_SECRET_A=
ALPACA_ENDPOINT=https://paper-api.alpaca.markets/v2
ALPACA_DATA_ENDPOINT=https://data.alpaca.markets/v2

# ── Alpaca — Account B (Claude Long Term / PA39DCU87MFL) ───────────────────
ALPACA_KEY_B=
ALPACA_SECRET_B=

# ── Research ───────────────────────────────────────────────────────────────
PERPLEXITY_API_KEY=
PERPLEXITY_MODEL=sonar-pro
FMP_API_KEY=

# ── AI ─────────────────────────────────────────────────────────────────────
ANTHROPIC_API_KEY=

# ── Notifications ──────────────────────────────────────────────────────────
NOTIFY_EMAIL=kenhhk@gmail.com
NOTIFY_PHONE=+16462343838
SMTP_USER=          # Gmail address for sending notifications
SMTP_PASS=          # Gmail App Password (not your regular password)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587

# ── TradingView Webhook Security ───────────────────────────────────────────
TV_WEBHOOK_SECRET=  # Generate a random string, use same value in TradingView alert JSON

# ── GitHub (webhook receiver only) ────────────────────────────────────────
GITHUB_TOKEN=       # Personal access token with repo write scope
GITHUB_REPO=kenhhk/ken-trading-bot
